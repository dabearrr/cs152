USE build.sh to build the parser
Use this command to parse code:
/.parser < inputcode.txt
then run the code like this:
./mil_run mil_code.mil

To automatically run my included tests (fib, primes, mytest, and semantic checks *that work) just do this:
./test.sh

Thanks for the great lab!